# 📖 Руководство пользователя — AutoClicker Suite

## Оглавление

1. [Установка и запуск](#установка-и-запуск)
2. [Интерфейс приложения](#интерфейс-приложения)
3. [Модуль мыши](#модуль-мыши)
4. [Модуль клавиатуры](#модуль-клавиатуры)
5. [Макро-движок](#макро-движок)
6. [Умные режимы](#умные-режимы)
7. [Профили и настройки](#профили-и-настройки)
8. [Горячие клавиши](#горячие-клавиши)
9. [Решение проблем](#решение-проблем)

---

## Установка и запуск

### Портативная версия (рекомендуется)

1. Скачайте `AutoClickerSuite-X.X.X-portable-win64.zip`
2. Распакуйте в любую папку
3. Запустите `AutoClickerSuite.exe`

Все настройки сохраняются рядом с программой — можно носить на флешке.

### Установщик

1. Скачайте `AutoClickerSuite-X.X.X-setup-win64.exe`
2. Запустите и следуйте инструкциям мастера установки
3. Программа создаст ярлык на рабочем столе и в меню Пуск

### Запуск от администратора

Для некоторых приложений (игры, полноэкранные программы) может потребоваться запуск от имени администратора:
- Правой кнопкой по `AutoClickerSuite.exe` → **Запуск от имени администратора**
- Или в свойствах ярлыка → **Совместимость** → **Запускать от имени администратора**

---

## Интерфейс приложения

Главное окно разделено на:

- **Верхняя панель**: меню (Файл, Вид, Помощь)
- **Центральная часть**: вкладки модулей (Мышь, Клавиатура, Макросы, Умные режимы)
- **Нижняя панель**: лог действий в реальном времени

При закрытии окна приложение **сворачивается в системный трей** (область уведомлений). Чтобы полностью выйти, используйте **правый клик по иконке в трее → Выход** или **Файл → Выход**.

---

## Модуль мыши

### Базовые настройки

| Параметр | Описание |
|----------|----------|
| **Кнопка мыши** | Левая (ЛКМ), Правая (ПКМ) или Средняя (СКМ) |
| **Тип клика** | Одиночный, Двойной или Удержание |
| **Интервал** | Время между кликами (от 1 мс до 1 часа) |

### Режимы позиционирования

- **Следование за курсором** — клик в текущей позиции мыши
- **Фиксированные координаты** — клик по заданным X, Y. Нажмите «Захватить» — у вас будет 5 секунд, чтобы навести курсор на нужную точку
- **Мультиточечный маршрут** — последовательность координат. Добавляйте точки через «Добавить точку» или захватывайте по одной

### Ограничения

- **По количеству кликов** — остановка после N кликов (0 = бесконечно)
- **По времени** — остановка через N секунд

### Рандомизация (анти-детект)

- **Рандомизация интервала** — случайный разброс ±% от заданного интервала
- **Рандомизация координат** — случайное смещение позиции в пикселях

---

## Модуль клавиатуры

### Режимы работы

#### 1. Одиночная клавиша
Автоматическое нажатие выбранной клавиши с заданным интервалом. Нажмите «Захватить клавишу» и нажмите нужную клавишу.

#### 2. Комбинация клавиш
Автоматическое нажатие комбинации (например, Ctrl+C). Выберите модификаторы (Ctrl, Shift, Alt, Win) и основную клавишу.

#### 3. Печать текста
Посимвольный ввод строки текста с настраиваемой задержкой между символами. Поддерживает Unicode и кириллицу.

#### 4. Клавиатурный макрос
Запись и воспроизведение последовательности нажатий клавиш. Нажмите «Начать запись» для записи макроса.

### Параметры

| Параметр | Описание |
|----------|----------|
| **Интервал** | Задержка между нажатиями (1 мс — 1 час) |
| **Количество** | Лимит нажатий (0 = бесконечно) |
| **Рандомизация** | Случайный разброс интервала ±% |

---

## Макро-движок

### Запись макроса

1. Нажмите **F7** или кнопку записи
2. Выполните действия мышью и клавиатурой
3. Нажмите **F7** ещё раз для остановки
4. Записанный макрос появится в редакторе

Во время записи фиксируются:
- Движения мыши (с фильтрацией мелких перемещений)
- Клики мыши
- Нажатия и отпускания клавиш
- Задержки между действиями

### Визуальный редактор

Редактор отображает макрос как список действий с цветовой кодировкой:
- 🔵 **Голубой** — действия мыши
- 🟢 **Зелёный** — действия клавиатуры
- 🟠 **Оранжевый** — задержки
- 🟣 **Фиолетовый** — циклы и управление

Доступные операции:
- **Перетаскивание** — изменение порядка действий
- **Добавление** — новые действия через панель параметров
- **Удаление** — выделите действие и нажмите «Удалить»
- **Редактирование** — выберите действие и измените параметры

### Типы действий

| Действие | Описание |
|----------|----------|
| **Клик мыши** | Клик указанной кнопкой по координатам |
| **Перемещение мыши** | Перемещение курсора в точку |
| **Нажатие клавиши** | Нажатие и отпускание клавиши |
| **Удержание клавиши** | Нажатие клавиши (без отпускания) |
| **Отпускание клавиши** | Отпускание удерживаемой клавиши |
| **Комбинация клавиш** | Нажатие комбинации (Ctrl+C и т.д.) |
| **Печать текста** | Посимвольный ввод строки |
| **Задержка** | Пауза (фиксированная или случайная) |
| **Начало цикла** | Начало области повторений |
| **Конец цикла** | Конец области повторений |
| **Подмакрос** | Вставка другого макроса |
| **Комментарий** | Пояснительный текст (не выполняется) |

### Циклы

Вложенные циклы позволяют повторять группу действий:
```
[Цикл: 5 раз]
    Клик мыши (100, 200)
    Задержка 500 мс
    [Цикл: 3 раза]
        Нажатие клавиши A
        Задержка 100 мс
    [Конец цикла]
[Конец цикла]
```
Максимальная глубина вложенности: 10 уровней.

### Скорость воспроизведения

Регулируется от **0.1x** (в 10 раз медленнее) до **10x** (в 10 раз быстрее).

### Экспорт / Импорт

Макросы хранятся как JSON-файлы. Используйте кнопки «Экспорт» и «Импорт» для обмена макросами.

---

## Умные режимы

### Привязка к окну

Все действия будут выполняться только когда указанное окно активно.

1. Нажмите «Выбрать окно»
2. Кликните по нужному окну
3. Включите привязку

### Поиск по цвету

Ожидание появления (или исчезновения) пикселя нужного цвета на экране.

1. Укажите цвет (можно захватить с экрана)
2. Настройте допуск (0–255 для каждого канала RGB)
3. Выберите действие при обнаружении (клик, двойной клик, перемещение, уведомление)
4. Область поиска: весь экран или привязанное окно

### Поиск по изображению

Поиск фрагмента изображения на экране (template matching).

1. Загрузите шаблон (скриншот фрагмента)
2. Настройте порог совпадения (0.0–1.0, рекомендуется 0.85+)
3. Выберите действие при обнаружении
4. Работает без OpenCV — встроенная NCC-реализация с пирамидальной оптимизацией

### Планировщик

Запуск макросов по расписанию.

1. Добавьте задачу
2. Укажите время запуска (дата + время)
3. Выберите макрос для выполнения
4. Опционально: интервал повтора (в минутах)

### Профили

Сохранение и загрузка всех настроек всех модулей:

- **Файл → Сохранить профиль** — сохранить текущие настройки
- **Файл → Загрузить профиль** — восстановить настройки

Профили хранятся как JSON-файлы в `%APPDATA%\AutoClickerSuite\profiles\`.

---

## Горячие клавиши

| Клавиша | Действие | Работает глобально |
|---------|----------|-------------------|
| **F6** | Старт / Стоп текущего действия | ✅ |
| **F7** | Начать / Остановить запись макроса | ✅ |
| **F8** | Экстренная остановка **всех** действий | ✅ |

Горячие клавиши работают даже когда приложение свёрнуто в трей.

---

## Решение проблем

### Клики не работают в определённом приложении

- Запустите AutoClicker Suite **от имени администратора**
- Некоторые приложения (особенно игры с античитом) могут блокировать SendInput

### Приложение не запускается

- Убедитесь, что установлен **Visual C++ Redistributable 2022** (x64)
- Проверьте, что все Qt DLL находятся рядом с .exe
- Попробуйте запустить от имени администратора

### Хоткеи не срабатывают

- Убедитесь, что другая программа не перехватывает F6/F7/F8
- Перезапустите приложение от имени администратора
- Проверьте, что приложение работает (иконка в системном трее)

### Высокое потребление CPU при поиске по изображению

- Увеличьте интервал поиска
- Уменьшите область поиска (привяжите к окну)
- Используйте меньший шаблон

### Лог заполняется слишком быстро

Лог автоматически ограничен 5000 строками. Файл лога: `%APPDATA%\AutoClickerSuite\logs\autoclicker.log`.

---

## Системные требования

| Параметр | Минимум | Рекомендуется |
|----------|---------|---------------|
| **ОС** | Windows 10 x64 | Windows 11 x64 |
| **RAM** | 50 МБ | 100 МБ |
| **CPU** | Любой x64 | — |
| **Диск** | ~30 МБ | ~50 МБ |
| **Разрешение** | 1024×768 | 1920×1080+ |
